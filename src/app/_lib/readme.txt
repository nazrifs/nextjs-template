private folders control:

- separating UI logic from routing logic
- consistently organize internal files across different projects
- for sorting and grouping files in editors
- avoid naming convention and conflicts